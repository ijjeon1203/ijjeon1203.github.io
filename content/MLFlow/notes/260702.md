Experiment 이름: 고정 (예: missile-tracking-ppo) — 대시보드 좌측 트리에 이걸로 묶여서 보임

Run 이름: 매 학습마다 새로 생성 (타임스탬프나 자동 생성된 랜덤 이름)

가중치 저장은 MLflow가 아니라 체크포인트 로직에 따라 동작 

tag에서 git_commit 확인
해당 run의 artifact 경로에서 가중치 파일 다운로드 (NAS 경로)