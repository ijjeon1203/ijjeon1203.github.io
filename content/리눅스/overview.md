# 리눅스 

Ctrl + A   # 줄 맨 앞으로
Ctrl + E   # 줄 맨 뒤로
Ctrl + R   # 히스토리 검색 (최강)
Ctrl + L   # 화면 정리
Alt  + .   # 이전 명령어의 마지막 인자

# 명령어

ls -alh
cd
pwd
cp / mv / rm
grep
find
less
tail -f
chmod
chown

man grep, grep --help에 익숙해지

# 쉘 환경
커스터마이징

sudo apt install zsh
chsh -s $(which zsh)


# 에러 해결 루틴 고정하기
- ❌ 에러 → 구글 → 복붙
- ⭕ 에러 → 로그 → 재현 → 원인 분리

dmesg
journalctl -xe
journalctl -u 서비스명



# 파일 시스템 
/etc     → 설정
/var     → 로그, 가변 데이터
/usr     → 프로그램
/home    → 사용자
/proc    → 커널 정보



# tmux는 개발자 보험
- SSH 끊겨도 세션 유지
- 터미널 여러 개 정리
- 서버 개발자 필수


tmux new -s work

리눅스 개발자 루틴 (현실판)

터미널 열기 (Ctrl+Alt+T)

작업 디렉토리 이동

tmux 시작

코딩/빌드

로그 확인

md로 기록


마우스를 버려라" - 터미널 단축키 익히기
리눅스 숙련도의 80%는 터미널에서 나옵니다. GUI 대신 명령어로 모든 것을 해결해 보세요.

Tab 키 활용 (자동 완성): 파일명이나 폴더명을 끝까지 칠 필요가 없습니다. 두 번 누르면 가능한 목록도 보여줍니다.

Ctrl + R (역방향 검색): 예전에 쳤던 긴 명령어가 기억나지 않을 때 키워드만 입력하면 찾아줍니다.

Ctrl + A / E: 커서를 줄 맨 앞이나 맨 뒤로 즉시 이동합니다.

Alias 설정하기: 자주 쓰는 긴 명령어는 별명(Alias)으로 만드세요.

예: alias hserve='hugo server -D' (터미널에 hserve만 쳐도 로컬 서버 실행)

---

# 리눅스 파일 시스템 계층 구조(FHS) 이해

리눅스는 "모든 것이 파일이다"라는 철학을 가지고 있습니다. 어디에 무엇이 있는지 아는 것이 중요합니다.경로역할/etc시스템 설정 파일 (설정 바꿀 때 가장 많이 방문)/var/log시스템 및 앱의 로그 (에러 원인 찾을 때 필수)/home사용자별 개인 파일 (현재 작업 중인 workspace가 위치할 곳)/bin, /usr/bin실행 가능한 명령어 프로그램들이 모여 있는 곳


3. "파이프(|)와 리다이렉션(>)" 마스터하기
여러 명령어를 조합해서 강력한 기능을 만드는 리눅스의 꽃입니다.

grep: 텍스트 검색. (예: ls -R | grep "post" -> 모든 하위 폴더에서 post가 포함된 파일 찾기)

tail -f: 로그 파일을 실시간으로 모니터링할 때 씁니다.

리다이렉션: ls > file_list.txt라고 치면 화면에 나올 내용이 파일로 저장됩니다.

4. 패키지 관리자와 친해지기
Windows처럼 설치 프로그램을 다운로드받는 게 아니라, 명령어로 관리하는 습관을 들이세요.

Ubuntu 계열: sudo apt update && sudo apt install [패키지명]

CentOS 계열: sudo yum install [패키지명]

내가 필요한 도구(git, hugo, python 등)를 명령어로 직접 설치하고 버전 관리까지 해보는 경험이 중요합니다.

5. 도움말(man)을 읽는 습관
모르는 명령어가 나오면 구글링도 좋지만, 터미널에 man [명령어]를 쳐보세요. 해당 명령어의 공식 설명서가 나옵니다.

"구글은 정답을 알려주지만, man page는 원리를 알려줍니다."

