
# git의 스냅샷
- 커밋의 기초가 됨
- 파일의 내용과 그 파일을 담고 있는 디렉터리 구조로 이뤄짐 


커밋(Commit)
- 체크인을 수행할 때 이전에 갱신된 내용이 있는 경우 충돌을 알리고 diff 도구를 이용해 수정 후 갱신 완료


git은 그 참조를 통해 과거의 모든커밋을 연쇄적으로 해석할 수 있음

- 커밋은 현재의 작업상태와 이전 버전으로부터의 변경사항 모두를 나타냄 
- 어ㄷ에있어왔는지를 아는데 중요하지만 어디로 가고 있는지를 아는덴 항상 도움이 되지않음 
- 이미 commit 한 소스 파일도 수정이 가능하다.

작업사본 복사해 버전 번호 부여하는 행위가 커밋 
그 파일의 정확한 상태를 기록 



커밋은 사후에 변경할수없음
커밋이란 공유를 의도함
커밋되지 않았다면 원래투버 없던일 
commit or it didn't happen

커밋은 객체라고하는 git 데이터의 한종류 

파일내용
폴더의 구조정보
프로젝트 버전표시하는 커밋

git이 알고있는 모든 정보는 내부적으로 객체에 저장
각 객체는 담고 있는 정보에 따라 고유한이름을갖음
객체의 이름 혹은 식별자는 단순히 이름이 아닌 객체슬 서로 구별하기 위해 컴퓨터가 판독할 수 있는 작은지문 

매일 수행하는작업과 관련된 유일한 객체는 커밋
의미론적으로 각 커밋은 주어진 어느 한 순간의 프로젝트 상태를 나타내는 완전하 스냅샷 

커밋의고요식별자 다른순간에 프로젝트파일이 보인모습으로부터 현재 순간의 상태를 구별할 수 있게 해줌 


git 에서도 이름이나 숫자등 원하는 방식사용 가능 
각커밋에 대해 고유 식별자를 부여 



커밋은 사후에 변경할수없음
커밋이란 공유를 의도함
커밋되지 않았다면 원래투버 없던일 
commit or it didn't happen

커밋은 객체라고하는 git 데이터의 한종류 

파일내용
폴더의 구조정보
프로젝트 버전표시하는 커밋

git이 알고있는 모든 정보는 내부적으로 객체에 저장
각 객체는 담고 있는 정보에 따라 고유한이름을갖음
객체의 이름 혹은 식별자는 단순히 이름이 아닌 객체슬 서로 구별하기 위해 컴퓨터가 판독할 수 있는 작은지문 

매일 수행하는작업과 관련된 유일한 객체는 커밋
의미론적으로 각 커밋은 주어진 어느 한 순간의 프로젝트 상태를 나타내는 완전하 스냅샷 

커밋의고요식별자 다른순간에 프로젝트파일이 보인모습으로부터 현재 순간의 상태를 구별할 수 있게 해줌 



# 사용법

각 커밋에 대해 고유 식별자를 부여 하여 git 에서도 이름이나 숫자등 원하는 방식사용 가능 



커밋 내용 
- 중요한 변경사항


  
# 기능
- 메모의 역할
- 이 메모가 어떤 사진에 대한 건지 즉
- 트리의 ID, 저자와 커미터, 부모커밋의 ID, 그리고 우리가 git commit -m 의 커밋메시지



커밋구성요소
커밋작업버전이되는원리
커밋하는방법

자신의git 워크플로를정립하기위한빌딩블록 

# 명령어
-a : 변경된 파일을 커밋에 추가

-m : 커밋메시지지정

-am 둘다합친것 


브랜치에 사용? 커밋에 관련이겟지?
- 나중에 추가 정리하면서 


# git

commit 취소하기
완료한 commit을 취소해야 할 때가 있다.
너무 일찍 commit한 경우
어떤 파일을 빼먹고 commit한 경우 이때, git reset HEAD^ 명령어를 통해 git commit을 취소할 수 있다.
// commit 목록 확인
$ git log
// [방법 1] commit을 취소하고 해당 파일들은 staged 상태로 워킹 디렉터리에 보존
$ git reset --soft HEAD^
// [방법 2] commit을 취소하고 해당 파일들은 unstaged 상태로 워킹 디렉터리에 보존
$ git reset --mixed HEAD^ // 기본 옵션
$ git reset HEAD^ // 위와 동일
$ git reset HEAD~2 // 마지막 2개의 commit을 취소
// [방법 3] commit을 취소하고 해당 파일들은 unstaged 상태로 워킹 디렉터리에서 삭제
$ git reset --hard HEAD^
https://gmlwjd9405.github.io/2018/05/25/git-add-cancle.html